interface:
  display_name: "Cross-Agent Handoff"
  short_description: "Reconcile coding work across agent sessions."
  default_prompt: "Use $cross-agent-handoff to create or update a repository-verified handoff pack for these agent sessions."
